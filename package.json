{
  "name": "askdkc.github.io",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node --test tests/**/*.test.mjs",
    "verify:build": "node scripts/verify-build.mjs",
    "content:convert": "node scripts/convert-org.mjs",
    "predev": "npm run content:convert",
    "precheck": "npm run content:convert",
    "prebuild": "npm run content:convert"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/node": "^26.4.0",
    "typescript": "^6.0.3"
  }
}
